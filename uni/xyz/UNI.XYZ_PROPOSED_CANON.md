# UNI.XYZ — BREATHE / REST / REACT proposed canon

Status: `VALIDATION`  
Promotion disposition: `HOLD`  
Target canonical structure version: `0.1.0`

Legal authority: Design Orchard LLC  
Operating DBA: DrMarchand’s Laboratory  
Operating environment: 🔬 DrMarchand’s Lab⚛︎ratory™  
Runtime: DrMarchand’s ⚙︎ Nɛuro-Forge Engine™

## Claim boundary

UNI.XYZ is an enforceable computational-law candidate. It is not promoted canon and does not claim to be a validated physical scientific law.

Scientific law describes a repeatable invariant under defined physical conditions. Enforceable code law prescribes what may, must, or cannot occur inside a governed computational jurisdiction.

## Symbols

- `A / Alpha` — aggressive force or first governed subject.
- `B / Beta` — calm restraint or comparison subject.
- `C / Command` — requested intervention; input is not authority.
- `Θ.REST()` — observation, recovery, evidence assimilation, and retained wisdom.
- `BREATHE()` — bidirectional boundary exchange.
- `Δ.REACT()` — authorized state transition with an evidence receipt.

## Canonical cycle

```text
Θ.REST()
  → BREATHE.IN()
  → C
  → AUTHORITY CHECK
  → Δ.REACT()
  → BREATHE.OUT()
  → EVIDENCE
  → Θ.REST()
```

An unauthorized command emits denial evidence and returns to REST without authoritative mutation.

## State signature

```text
State = (ΔA, ΔB, Difference, Connection)
```

The four Boolean observations produce sixteen configurations. Change, difference, and connection remain independent:

- Unchanged does not mean equal.
- Together does not mean identical.
- Separate does not mean different.

## Expected versus observed

- Expected change + observed change → `CONFIRMED_CAPABILITY_CANDIDATE`
- Expected change + no observed change → `FAILURE_OR_RESISTANCE`
- No expected change + observed change → `SIDE_EFFECT_OR_DISCOVERY`
- No expected change + no observed change → `STABILITY_OR_INERT`

Classification is enforced by schema conditions and verified by runtime tests.

## Phase model

```text
Phase = f(Freedom, Relationships, Energy, Pressure, Constraints, Geometry, Tolerance)
```

`PARTICLE`, `GAS`, `LIQUID`, and `SOLID` require numeric measurements or test doubles. A label alone is invalid.

## Lionheart boundary

Lionheart remains architectural-integrity enforcement and does not execute business logic. The `IN / REST / REACT / OUT` circulation operates beneath that boundary.

Before approval or publication, Lionheart checks:

1. Identity
2. Canon
3. Reflection
4. Drift
5. Gate authority

Any failure blocks movement. Big Brother observes and reports; it cannot silently rewrite authoritative state.

## Platform routing

Liquid Logic is the semantic reference representation. Platform views submit commands and render governed state; they do not directly mutate authoritative objects.

- Apple: Liquid Logic → Phoenix → Swift/SwiftUI
- Google: Liquid Logic → ChromeOS/Bootstrap → semantic round-trip
- Windows: Liquid Logic → Windows adapter
- Android: Liquid Logic → Android adapter

Object identity, authority, permitted commands, evidence requirements, and semantic behavior remain invariant. Pixel identity is not required. A semantic difference requires a recorded Delta.

## Lifecycle law

```text
WORKING
  → PROPOSED_PROMOTION_DRAFT
  → VALIDATION
  → APPROVED
  → CANONICAL
  → SUPERSEDED
```

- `APPROVED` requires all gates, verified evidence, and an authority receipt from an authorized human acting for Design Orchard LLC.
- `CANONICAL` additionally requires a write-authorized GitHub publication, commit and blob readback, artifact digest, publication receipt, and required cross-system readbacks.
- Failed validation returns to `PROPOSED_PROMOTION_DRAFT` for remediation.
- Canonical objects may only move to `SUPERSEDED`; history is preserved.

## Canonical-source law

After authorized merge, the pinned file in `DrMarchand/DrMarchand-OS` is the canonical architectural source. Drive and OneDrive copies are derivatives or sealed mirrors identified by source revision and digest. Asana remains an execution mirror. External IDs are locators, not authority.

## Current disposition

Local artifact, runtime, schema, role-boundary, and checksum validation may advance the package into `VALIDATION`. Canonical promotion remains blocked until the Asana identity, HubSpot portal identity, iCloud portability, GitHub merge/readback, and final authority receipt all pass.

## Closing law

REACT creates experience. BREATHE carries the exchange. REST takes in what worked and what did not, converts evidence into wisdom, and prepares the next lawful reaction.
